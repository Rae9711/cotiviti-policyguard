[project]
name = "cotiviti-policyguard"
version = "2.0.0"
description = "PolicyGuard Intelligence Studio — source-grounded policy review POC"
readme = "README.md"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
